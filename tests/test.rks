; Starting values
ldi @a 0
ldi @b 1

; Limit the sequence so it doesn't go on forever
ldi @c 6765
psh @c

.fibonacci
; Calculate the next number in the sequence
add @a @b
poke @a 0xFFFE
swp @a @b

; Print a newline
ldi @c 10
poke @c 0xFFFF
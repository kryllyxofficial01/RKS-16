#def limit !10
#def fizz !3
#def buzz !5
#def newline !10

; Setup
lda !3
sta @b

jmp .fizz

; Checks if the current number is divisible by 3
.label fizz
	ldb $fizz

	div @a @b
	mul @a @b
	lrb @c
	sub @b @a

	cmp @c @b

.label check
	hlt
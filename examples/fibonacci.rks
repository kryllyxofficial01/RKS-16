; Set a cap on the highest number
#def limit 144

; Initialize starting values
lda !0
ldb !1
ldc $limit

; Main loop
.label loop
	; Fixes timing issues
	nop

	add @a @b
	out @b
	swp @a @b

	cmp @c @a
	jnz .loop